package org.vizzini.ai.neuralnetwork;

/**
 * Defines methods required by an input filter for a neural network.
 * 
 * @param <I> Input type.
 */
public interface InputFilter<I> extends Filter<I, double[]>
{
    // Nothing to do.
}
