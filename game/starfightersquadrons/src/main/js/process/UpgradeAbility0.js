/*
 * Provides upgrade abilities for Events.
 */
define(function()
{
    "use strict";
    var UpgradeAbility0 = {};

    if (Object.freeze)
    {
        Object.freeze(UpgradeAbility0);
    }

    return UpgradeAbility0;
});
