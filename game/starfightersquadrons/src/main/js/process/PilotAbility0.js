/*
 * Provides pilot abilities for Events.
 */
define(function()
{
    "use strict";
    var PilotAbility0 = {};

    if (Object.freeze)
    {
        Object.freeze(PilotAbility0);
    }

    return PilotAbility0;
});
