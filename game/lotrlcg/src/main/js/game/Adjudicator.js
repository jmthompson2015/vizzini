define(function()
{
    "use strict";
    function Adjudicator()
    {
        this.determineWinner = function(environment)
        {

        };

        this.isGameOver = function(environment)
        {
            return false;
        };
    }

    return Adjudicator;
});
